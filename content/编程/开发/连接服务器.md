通过ssh连接
可以使用remote ssh插件(来自vscode插件市场)
trae的ssh功能就是通过这个实现的, 但是版本较高, 不支持一些低配的系统, 比如学校集群的centos7

可选方案: 下载老版本的romote ssh插件, 再用vscode去连接, vscode copilot可以通过某些方式调用自定义接口

# 奇怪的情况
conda换成清华源后疑似无法找到对应包索引, 不知道是无法访问清华源还是其他问题, 但通过default的anaconda源也是可以的. 但是下载好了之后自检查发现有很多缺失文件.

问题自行解决了, 应该是管理员处理了

