### 推送单个文件
```bash
git add run_cdcl_bench.sbatch
```

### 推送文件夹中的所有文件
```bash
git add Logs/
```

### 提交
```bash
git commit -m "Update Makefile, add solver binary, scripts and logs"
```

### 最后git push即可