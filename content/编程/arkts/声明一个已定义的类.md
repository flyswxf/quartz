当显式的给出了类的constructor, 并定义了对应的interface, 可以通过**键值对**的形式构造
```ts
newClass:myClass = new myClass({
	attr1:...,
	attr2:...
})
```

当显式的给出类的constructor时, 但没有定义interface, 则不能通过键值对的形式, 只能**直接传参**, 这就会**不清晰**
```ts
newClass:myClass = new myClass(...,...)
```

当没有显示的给出类的constructor时, 
还可以用Object()来构造 #todo