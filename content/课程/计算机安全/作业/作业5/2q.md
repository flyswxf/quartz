### 数据库加密的应用级别

根据资料，加密可以应用于数据库的以下不同级别：

1. **整个数据库级别（Entire database）**
2. **记录级别（Record level）**：加密选定的记录（即对特定的行进行加密）。
3. **属性级别（Attribute level）**：加密选定的列（即对特定的属性进行加密）。
4. **单个字段级别（Individual field）**：对单独的某个数据字段进行加密。