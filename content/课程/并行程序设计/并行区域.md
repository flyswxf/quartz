并行区域. 这个区域中是多线程并行的, 在区域外是一个主线程线性执行的.

- 并行区域不能相交, 但是并行区域可以嵌套
- [[OpenMP]]不区分
- 并行区域外的变量默认是**共享**的, 可以通过
- 并行区域内的变量是**私有**的

### 表示方法
用`{}`包括
```
#pragma omp parallel
{
	
}
```
如果只有一条语句, 可以不加`{}`
比如
```
#pragma omp critical
	func();
```