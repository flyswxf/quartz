## 定义
- 将一个被测因素的输入域按“行为相同或近似相同”分组，每组称为一个等价类。
- 在同一类中任取一个代表值进行测试，期望程序表现一致，从而减少用例数量。

### 分类
- 有效等价类: 符合被测因素的约束
- 无效等价类: 不符合被测因素的约束(即异常值)

### 识别被测因素
- 输入
- 输出
- 被测因素之间的关系
## 例子
对于某个函数, 输入变量x需要在\[5,10\]区间内

可以将x的输入域划分为: <5, 5-10, >10 三个等价类, 其中<5和>10是无效等价类, 5-10是有效等价类