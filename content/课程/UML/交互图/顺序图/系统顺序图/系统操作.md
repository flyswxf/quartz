## 定义
系统操作是对系统事件的内部处理逻辑的抽象。当系统接收到一个事件（如 `enterItem`）时，它会调用对应的系统操作。

## 描述方式
系统操作的详细行为通常通过[[操作契约]]来描述
## 示例
对应于 POS 系统的 `enterItem(itemID, quantity)` 系统事件，其系统操作可能包含以下逻辑（在操作契约中描述）：
- 根据 `itemID` 查找商品描述 (ProductDescription)。
- 创建一个新的销售条目 (SalesLineItem)。
- 将该条目关联到当前的销售交易 (Sale) 中。
- 更新销售总额。
